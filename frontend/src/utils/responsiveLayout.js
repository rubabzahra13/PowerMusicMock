export const formGridClass = 'grid grid-cols-1 gap-3 sm:grid-cols-2';

export const adminPageShellClass =
  'mx-auto flex h-full min-h-0 min-w-0 w-full max-w-7xl flex-col overflow-hidden';

export const adminPageShellClassNarrow =
  'mx-auto flex h-full min-h-0 min-w-0 w-full max-w-4xl flex-col overflow-hidden';

/** @deprecated Prefer AdminPageScroll — kept for pages that still need a plain class. */
export const adminPageScrollClass =
  'mx-auto h-full min-h-0 min-w-0 w-full max-w-7xl overflow-y-auto space-y-4 sm:space-y-6 select-none';
